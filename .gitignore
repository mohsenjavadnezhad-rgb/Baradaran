# به‌درخواستِ صریحِ کاربر: همه‌چیزِ سایت آپلود می‌شود، فقط نویز سیستم‌عامل/
# ویرایشگر نادیده گرفته می‌شود. توجه: این یعنی رمزِ واقعیِ دیتابیس
# (includes/config.php، server-config.php) و رمزِ پنلِ ادمین (reset-admin.php)
# هم در ریپو می‌مانند.
Thumbs.db
.DS_Store

# تنظیماتِ محلیِ خودِ ابزار Claude Code — جزوِ اطلاعاتِ سایت نیست
.claude/
