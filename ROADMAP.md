# نقشهٔ راهِ پروژه (Roadmap)

این فایل، فهرستِ کاملِ کارهایی است که تا امروز رویِ **فروشگاه برادران /
yadakii.ir** انجام شده — بخش‌بندی‌شده بر اساسِ حوزهٔ کاری، نه لزوماً به
ترتیبِ زمان. هدف این است که هروقت خواستید کار را ادامه بدهید، از همین فایل
شروع کنید: ببینید چه چیزی آماده است، چه تصمیمی پشتِ هرکدام بوده، و چه مواردِ
بازی هنوز روی میز مانده (بخشِ آخر).

جزئیاتِ فنیِ خیلی دقیق‌تر (چرا یک تصمیم گرفته شده، کجایِ کد باید تغییر کند)
در حافظهٔ کاریِ دستیار نگه‌داری می‌شود، نه اینجا — این فایل خلاصه و برایِ
مرورِ سریع است. برایِ شرحِ ساختارِ فنیِ پروژه و نصب/استقرار، به [README.md](README.md) نگاه کنید.

---

## زیرساخت و ابزار

- [x] **روشِ استقرار روی هاستِ زنده**: آپلودرِ اختصاصیِ `up.php` (بدونِ نیاز به FTP/SSH) + گردشِ کارِ AMSI-safe در PowerShell برایِ رمزگذاریِ base64 و آپلود
- [x] **مهاجرتِ تدریجیِ دیتابیس**: الگویِ `migrate-*.php` — هر قابلیتِ تازه پشتِ یک هلپرِ `*Ready()` در `functions.php` است، پس نبودِ مهاجرت یعنی خاموش‌ماندنِ قابلیت، نه خطا
- [x] **کتابخانهٔ آیکونِ SVG** (`includes/icons.php`) — جایگزینِ همهٔ ایموجی‌های سایت، قاعدهٔ ۲۴×۲۴ لبه‌به‌لبه
- [x] **نسخه‌بندیِ Cache-busting داراییِ استاتیک**: `?v=N` رویِ `style.css`/`main.js`/`cart.js`/`search.js` — باید در ۹ فایلِ ارجاع‌دهنده هم‌زمان تغییر کند (نسخهٔ فعلی: **v51**)
- [x] **مخزنِ Git + سینک با گیت‌هاب** (`github.com/mohsenjavadnezhad-rgb/Baradaran`, خصوصی) — به‌درخواستِ صریحِ کاربر، تمامِ فایل‌هایِ توسعه/دیباگ هم شاملِ رمزهایِ زنده نگه‌داری شده‌اند (نگاه کنید به بخشِ «یادداشت‌های امنیتی» در README)
- [x] **مستندسازی**: `README.md` کاملِ فارسی (معرفیِ پروژه، ساختار، نصب، استقرار)

## صفحهٔ خانه و بنرها

- [x] صفحهٔ خانه (`banners.php`) از فروشگاه جدا شد — فقط بنر است، «خانه» عمداً به آن اشاره می‌کند
- [x] چیدمانِ ثابت: بنرِ اصلیِ تمام‌عرض + ردیفِ همیشه-سه‌تاییِ زیرِ آن (فروشِ ویژه / آفرهای زمان‌دار / جایگزارِ خالی)
- [x] لایهٔ تزیینیِ گوشه‌ها با ۹ سبکِ متحرکِ SVG (چرخ‌دنده، ابزار، لاستیک، ماشین، جاده، موتور/پیستون، نئون، شناور) — قابلِ تنظیم از پنل
- [x] **آفرِ زمان‌دار**: جدولِ `timed_offers`، اسلایدرِ خودکار با فاصلهٔ قابلِ‌تنظیم، شمارش‌معکوسِ روز/ساعت، نوارِ زمانِ باقی‌مانده، نشانِ «فروخته شد»، آفرِ تمام‌شده از صفحه برداشته نمی‌شود (فقط برچسب می‌خورد)
- [x] **میانبرِ «آفر»**: کلید کنارِ هر محصول در `admin/products.php` که مستقیم به فرمِ «آفر جدید» با همان محصولِ ازپیش‌انتخاب‌شده می‌رود؛ همان‌جا قیمت/درصدِ تخفیفِ محصول هم قابلِ ویرایش است (مستقیماً رویِ خودِ محصول می‌نشیند، نه یک ستونِ جدا)
- [x] رنگِ کلیدها/بنرهایِ آفر به تمِ مشکی/طلاییِ سایت (`#FFBE00`) هماهنگ شد

## فروشگاه سمتِ مشتری

- [x] دو مسیرِ مستقلِ مرور: **فروشگاه** (`shop.php`، بر اساسِ برند/مدلِ خودرو) و **دسته‌بندیِ قطعات** (`parts.php`، بر اساسِ نوعِ قطعه مثلِ «موتور و قطعات موتوری»)
- [x] منویِ اصلی: «فروشگاه» → `parts.php` (دسته‌بندیِ قطعات، بدونِ شمارهٔ زیرشاخه، کارت‌هایِ یک‌اندازه)، «قطعات خودرو» → `shop.php` (برندها) — این دو عمداً از هم جدا شدند
- [x] کلیک رویِ یک برند: برچسبش با پروازِ نرم به کنارِ دکمهٔ «همه برندها» می‌رود و همان‌جا (به‌شکلِ یک تگِ واقعی، نه فقط متن) تا کلیکِ بعدیِ «همه برندها» می‌ماند
- [x] نمایشِ نامِ انگلیسیِ برند (اسلاگ) با سه حالتِ قابلِ‌تنظیم از پنل: نشان‌نده (پیش‌فرض) / فقط با موس / همیشه — با حروفِ اولِ بزرگ
- [x] جست‌وجویِ زندهٔ ای‌جکسی با دیبانس، برجسته‌کردنِ عبارت، برچسبِ برند/دسته‌بندی، قیمت — نوارِ جست‌وجویِ تکراری از `search.php` حذف شد
- [x] صفحهٔ محصول: گالری، امتیاز و نظرات، پرسش‌وپاسخ، تخفیفِ جزئی/کلی، تخفیفِ هر متغیر (رنگ/سایز/کشورِ سازنده)
- [x] چاپ/خروجیِ اکسل برایِ هر دو سیستمِ دسته‌بندی

## سبدِ خرید و تسویه‌حساب

- [x] سبدِ خرید: افزایش/کاهشِ تعداد بدونِ پرشِ ظاهریِ ردیف (`table-layout: fixed` + پهنایِ صریحِ ستون‌ها)
- [x] تسویه‌حسابِ دوستونه با تقویمِ شمسیِ دست‌نویس برایِ زمان‌بندیِ تحویل
- [x] **روش‌هایِ ارسال**: چند روش با پس‌کرایه، جعبهٔ باربری، **نرخ‌نامه** بر اساسِ شهر+وزن (اختیاری رویِ هر محصول)
- [x] **مالیات**: درصدِ اختیاری رویِ هر محصول، جمع‌شونده در سبد/تسویه‌حساب/فاکتور، ایتمایز روی فاکتور
- [x] **بررسیِ عکسِ نمونهٔ قطعه** پیش از خرید: سه کادرِ آپلودِ تمیز، بدونِ پاپ‌آپِ تأییدِ اضافه، با چند راهِ فرار که هرگز فروش را نمی‌بندد
- [x] **گیتِ کاملاً مسدودکنندهٔ «در انتظار بررسی موجودی» پیش از ثبتِ سفارش** (۲۰۲۶-۰۸-۳۰): بینِ بررسیِ عکسِ قطعه و تسویه‌حساب، برایِ هر نوع خرید (مشتری و همکار)، یک کادرِ بزرگِ متحرک (چراغ‌چشمک‌زن) که تا تأییدِ موجودیِ ادمین اصلاً اجازهٔ ورود به تسویه‌حساب نمی‌دهد — بدونِ راهِ فرار یا timeout؛ به‌محضِ تأیید، خودکار (چکِ سبزِ متحرک) وارد ثبتِ سفارش می‌شود. حتی «رد کردنِ مرحلهٔ عکس» دیگر بایپس نیست، فقط یعنی صفِ انتظار بدونِ عکس
- [x] **دو کلیدِ مستقلِ غیرفعال‌سازی برای ادمین** (همان روز): `این مرحله فعال باشد` (کلِ دو صفحه را کاملاً کنار می‌گذارد؛ سبد خرید → مستقیم تسویه‌حساب) و `تأییدِ موجودی برای ادامه الزامی باشد` (فقط صفحهٔ در-انتظار-موجودی را کنار می‌گذارد، صفحهٔ بررسی عکس می‌ماند) — هر دو در `admin/settings.php?sec=pchk`
- [x] **پیش‌فاکتور و فاکتورِ چاپی**

## پرداخت

- [x] لایهٔ درایورِ درگاه (`includes/payment.php`): زرین‌پال، زیبال، آیدی‌پی، درگاهِ آزمایشیِ داخلی — تعویضِ درگاهِ واقعی فقط با واردکردنِ اطلاعات و خاموش‌کردنِ حالتِ آزمایشی، بدونِ تغییرِ کد
- [x] کارت‌به‌کارت با فرمِ ثبتِ اطلاعاتِ واریز و تأییدِ دستیِ ادمین
- [x] **درگاهِ اعتباری/اقساطی** (BNPL): گزینهٔ دومِ مستقل از درگاهِ بانکی، درایورِ عمومی (provider-agnostic)، **پیش‌فرض خاموش** تا قراردادِ واقعی بسته شود؛ تنظیماتش حالا صفحهٔ جداگانهٔ خودش را دارد (`?sec=paycredit`)، جدا از «درگاهِ پرداخت»
- [x] روش‌هایِ ویژهٔ همکارانِ تأییدشده: «پرداختِ اول ماه» و «چک» (با آپلودِ عکسِ نمونهٔ چک، تفکیکِ «دریافتِ چک» از «پرداخت‌شده»)
- [x] **مرحلهٔ «بررسیِ موجودی»**: اولین قدمِ پیگیریِ سفارش؛ تا تأییدِ ادمین (یا تأییدِ ضمنی از طریقِ بررسیِ عکسِ قطعه)، هیچ روشِ پرداختی — حتی درگاهِ آنلاین — فعال نمی‌شود
- [x] **چک/پرداخت‌اولِ‌ماه دیگر منتظرِ تأییدِ پرداخت نمی‌مانند**: کلیدِ عمومیِ «پرداخت شد» از هر دو روش برداشته شد (تسویهٔ واقعی‌شان جای دیگری، در پیگیریِ تسویهٔ همکاران، پیگیری می‌شود) — برچسبِ چک پیش از دریافت شد «در انتظار دریافت چک»، و هم در `order-success.php` هم در حسابِ کاربری یادآوریِ صریح اضافه شد که ارسال/تحویلِ فیزیکیِ چک با ثبتِ آنلاینِ مشخصاتش فرق دارد
- [x] **فرمِ بانک/شماره/تاریخ/مبلغِ چک به‌کلی حذف شد**: انتخابِ «چک» در تسویه‌حساب دیگر هیچ فیلدی نمی‌خواهد — فقط یک پیغامِ تأییدِ سفارش + مهلتِ ارسالِ فیزیکیِ چک (روزِ مهلت و متنِ پیغام از تنظیماتِ ادمین قابل‌ویرایش‌اند)، به‌علاوهٔ کلیدِ «بازگشت به سبد» به‌جایِ فرمِ ویرایش

## پیگیریِ سفارش

- [x] مراحلِ زنجیره‌ای و قابلِ‌تنظیم (بررسیِ موجودی → تأییدِ سفارش → جمع‌آوری → یافتنِ کالا → پیک → تحویل به پست → ارسال‌شده)، هرکدام با پیامکِ خودکارِ خودش (کلیدِ خاموش/روشنِ مجزا)
- [x] نمایشِ مرحله‌به‌مرحله هم برایِ مشتری هم برایِ ادمین، با تشخیصِ روشِ ارسال برایِ اینکه کدام مرحله‌ها اصلاً معنا دارند
- [x] شمارهٔ سفارش به فرمتِ خوانا (سال‌ماه‌روز-شناسهٔ یکتا)

## حسابِ کاربری، مشتری و همکار

- [x] ورودِ مشتری با کدِ پیامکی (OTP)، با کلیدِ خاموش/روشنِ سراسری برایِ ورودِ بدونِ OTP
- [x] تفکیکِ بصریِ اکانتِ «همکار» از «مشتری» در نوار بالایِ سایت
- [x] **قیمت‌گذاریِ عمده برایِ همکاران بدونِ نیاز به تأییدِ ادمین** (تصمیمِ آگاهانه، نه سهو)
- [x] شمارهٔ‌قطعهٔ خودکار، قراردادِ اسلاگِ MVM، پیش‌فاکتور
- [x] **پیگیریِ تسویهٔ همکاران** در پنل: چه مشتری/همکاری چقدر بدهکار است و با چه روشی تسویه می‌کند
- [x] `cart_owner` — سبدِ خرید بینِ حساب‌ها به‌درستی منتقل/جدا می‌شود (سبدِ مهمان با ورود جذب می‌شود، سبدِ حسابِ دیگر دور ریخته می‌شود)

## پنلِ مدیریت — سایر

- [x] دو سیستمِ دسته‌بندیِ مستقل با CRUD کامل: برند/مدل (`categories.php`، طراحیِ گروه‌بندی‌شده/جمع‌شونده) و دسته‌بندیِ قطعات (`part-categories.php`)
- [x] مدیریتِ سفارش‌ها: تب‌هایِ فیلتر، جدولِ خواناتر (چند دورِ اصلاح رویِ چیدمانِ ستون‌ها و پهنایِ صفحه)
- [x] صفحهٔ شرایط و قوانین + ویرایشگرِ آن در تنظیمات
- [x] بخش‌هایِ تنظیماتِ سایت: فوتر، تزیینات، پیامک، درگاهِ پرداخت، پرداختِ اعتباری (جدا)، روش‌هایِ ارسال، نرخ‌نامه‌هایِ ارسال، بررسیِ عکسِ قطعه، شرایط و قوانین — هرکدام صفحهٔ ذخیرهٔ مستقلِ خودش را دارد تا ذخیرهٔ یک بخش، بخش‌هایِ دیگر را خالی نکند
- [x] کلیدِ سراسریِ فعال/غیرفعال‌سازیِ اطلاع‌رسانیِ پیامکیِ مراحلِ ارسال (برایِ وقتی پنلِ پیامک شارژ ندارد)

## تاریخچهٔ رفعِ اشکال‌هایِ قابلِ‌توجه

بیشترشان چند-دوره‌ای بودند (یک اصلاح، بازخوردِ کاربر، اصلاحِ بعدی) — فقط
عنوان‌ها اینجا آمده، جزئیاتِ فنی در حافظهٔ کاری است:

- [x] باگِ کلیکِ برند در فروشگاه (به‌اشتباه به صفحهٔ اصلی می‌رفت)
- [x] پرشِ ظاهریِ ردیفِ سبدِ خرید هنگامِ تغییرِ تعداد
- [x] کلیدِ حذفِ سبد که نصفه دیده می‌شد
- [x] واحدِ «تومان» که در جمعِ سبد قطع می‌شد
- [x] تب‌هایِ بی‌استایلِ مدیریتِ سفارش‌ها
- [x] ستونِ عملیاتِ مدیریتِ سفارش‌ها که کلیدهایش جا نمی‌شدند (چند دور، آخرین علتش: قابِ صفحه به‌اندازهٔ کافی عریض نشده بود)
- [x] باگِ فعال‌سازیِ مجددِ آفر (محصولِ غیرفعال از دراپ‌داون گم می‌شد)
- [x] فونتِ اشتباهِ دکمهٔ «همه برندها» (چون `<button>` برخلافِ `<a>` فونت را ارث نمی‌برد)
- [x] فلشِ لحظه‌ایِ تگِ برند پیش از محوشدن (به‌خاطرِ یک فریم تأخیر در مخفی‌کردنِ اصل)
- [x] تگِ تکراریِ برند بعد از انتخاب (هم بالا هم کنارِ «همه برندها» نشان داده می‌شد)

## موارد بازِ روی میز

- [ ] **«قیمت رو هم اشتباه حساب میکنه»** — گزارشِ مبهمِ کاربر دربارهٔ محاسبهٔ اشتباهِ قیمت (احتمالاً در سبد یا تسویه‌حساب)؛ سؤالِ روشن‌سازی رد شد و کاربر به موضوعِ دیگری پرید. **هنوز مشخص نیست دقیقاً کجا و در چه شرایطی رخ می‌دهد** — هروقت جزئیاتِ بیشتری (کدام صفحه، کدام محصول، چه عددی انتظار می‌رفت) در دسترس بود، باید دوباره بررسی شود.
- [ ] **درگاهِ اعتباری/اقساطی هنوز خاموش است** — کد و رابطِ کاربری آماده‌اند، فقط منتظرِ بستنِ قراردادِ واقعی با یک ارائه‌دهندهٔ BNPL و واردکردنِ اطلاعاتش در `?sec=paycredit` است.
- [ ] هیچ چیزِ دیگری از این جلسات به‌عنوانِ «نیمه‌کاره» علامت نخورده — هر درخواستی که مطرح شده تا انتها ساخته، دیپلوی، و روی هاستِ زنده تأیید شده.

---

**برایِ ادامهٔ کار:** این فایل را با من به اشتراک بگذارید یا فقط بگویید
«فلان موردِ باز را انجام بده» / «فلان بخش را گسترش بده» — با خواندنِ این
فایل و README.md، زمینه دوباره کامل می‌شود.
